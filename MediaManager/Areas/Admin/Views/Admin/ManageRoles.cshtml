@model MediaManager.Areas.Admin.ViewModels.ManageRoleViewModel
@{
    ViewBag.Title = "Manage Roles";
    Layout = MediaManager.Infrastructure.Helpers.SharedPages.LayoutPage;
}
<h2>@ViewBag.Message</h2>
<script type="text/javascript">
    var SearchAllDetails = '@Url.Action("ManageDatarole", "Admin", new { area = "Admin" })';
    var SaveAllDetails = '@Url.Action("SaveRoleDesc", "Admin", new { area = "Admin" })';
    var GetAllTasksDetails = '@Url.Action("GetAllTasks", "Admin", new { area = "Admin" })';
    var GetRoleTasksDetails = '@Url.Action("GetRoleTasks", "Admin", new { area = "Admin" })';
</script>



<script src="~/Content/Scripts/Admin/ManageRoles/ManageRole.js" type="text/javascript">
</script>

@using (Html.BeginForm())
{
 <form action="#" method="post">
 <table cellpadding="0" cellspacing="0" id="Layout" width="100%" class="Layout">
    <tr>
        <td>
            <div class="mainHeading">
               Manage Roles
            </div>
            <div id="SearchInputDiv" class="PlaceHolder">
                <table class="tableLayout">
                    <tr>
                        <td>
                        </td>
                    </tr>
                    <tr>
                        <td>
                            <div id="teamGrid" class="GridContainer" style="height: 410px; ">
                            </div>
                       </td>
                    </tr>
                    <tr>
                        <td align="right">
                            <input type="button" id="btnmanagerole" value="Manage Task For Role" name="btnmanagerole" class="inputButton"/>            
                            <input type="button" id="btnsave" value="Save" name="btnsave" onclick="find_click()" class="inputButton" />
                        </td>
                    </tr>
                </table>
            </div>
        </td>
    </tr>
</table>

     
     <div id="dialog"  class='PlaceHolder' style='display:none;border: 2px solid red; margin:0 auto;vertical-align:middle;' >
      <table width="100%" border="2">
          <tr>
              <td>
                 <select id="listbox3" multiple="multiple" style="height:200px; width:350px;">
                    @if (Session["TaskList"] != null)
                    {
                        foreach (var DisplayTask in HttpContext.Current.Session["TaskList"].ToString())
                        {
                            <option >@DisplayTask.ToString().ToList()</option>
                        }
                    }
                    </select>
                    <br />
                    <span id="box1Counter" class="countLabel"></span>
                </td>
                <td>
                    <button id="btn1" type="button">&nbsp;>&nbsp;</button> &nbsp;
                    <br />
                    <button id="btn2" type="button">&nbsp;>>&nbsp;</button> &nbsp;
                    <br />
                    <button id="btn3" type="button">&nbsp;<<&nbsp;</button>&nbsp; 
                    <br />
                    <button id="btn4" type="button">&nbsp;<&nbsp;</button>&nbsp;

                </td>
                <td align="right">
                <select id="listbox4" multiple="multiple" style="height:200px; width:350px;">
                       @if (HttpContext.Current.Session["TaskRolelist"] != null)
                       {
                           foreach (var DisplaytaskRole in this.Session["TaskRolelist"].ToString().ToList())
                           {
                               <option value="@DisplaytaskRole.ToString()">This is 2nd listbox</option>
                           }
                       }
                    </select>
                    <br />
                    <span id="box2Counter" class="countLabel"></span>
                </td>
            </tr>
        </table>
    </div>
     
  

 </form>
}

